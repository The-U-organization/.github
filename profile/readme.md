# U
actually usable language coming from the original U# (u mess)


so U (or whatewer you want to call it like) is meant to be simple, explicit and predictable. It's not some magic, I'm not a wizard. Also you can't expect overloaded libraries by default, though you can extend it. 

